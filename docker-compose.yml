version: '3.8'

services:
  discord-bot:
    build: .
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - GUILD_ID=${GUILD_ID}
      - WELCOME_CHANNEL_ID=${WELCOME_CHANNEL_ID}
    restart: unless-stopped
